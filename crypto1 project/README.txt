This repo contains two programs to hash and compare hashed files in given input and output directories (default folders: input, output).

To run, simply download the files with python installed, open the folder in console/terminal, and enter:
>python crypto1.py
>python crypto2.py
